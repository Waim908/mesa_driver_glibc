name: build mesa driver

on:
  workflow_dispatch:

jobs:
  ubuntu:
    runs-on: ubuntu-22.04-arm
    steps:
      - name: get patch
        run: |
          pwd
          sudo apt install tree -y
          tree


